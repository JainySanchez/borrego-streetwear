{{ 'component-price.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.72 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.78 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

{%- liquid
  assign products_to_display = section.settings.products_to_show
-%}

<section class="hanan-editorial-collection" id="hanan-editorial-collection-{{ section.id }}">
  <div class="hanan-editorial-collection__inner section-{{ section.id }}-padding">
    <div class="hanan-editorial-collection__top">
      <div class="hanan-editorial-collection__header">
        {% if section.settings.title != blank %}
          <h2 class="hanan-editorial-collection__title">
            {{ section.settings.title }}
          </h2>
        {% endif %}

        {% if section.settings.description != blank %}
          <div class="hanan-editorial-collection__description">
            {{ section.settings.description }}
          </div>
        {% endif %}
      </div>

      <div class="hanan-editorial-collection__controls">
        <button class="hanan-editorial-collection__arrow hanan-editorial-collection__arrow--prev" type="button" aria-label="Previous products">
          <span>‹</span>
        </button>

        <button class="hanan-editorial-collection__arrow hanan-editorial-collection__arrow--next" type="button" aria-label="Next products">
          <span>›</span>
        </button>
      </div>
    </div>

    <div class="hanan-editorial-slider-wrapper">
      <div class="hanan-editorial-slider">
        {% if section.settings.collection != blank and section.settings.collection.products.size > 0 %}
          {% for product in section.settings.collection.products limit: products_to_display %}
            <a href="{{ product.url }}" class="hanan-product-card" style="--delay: {{ forloop.index0 }};">
              <div class="hanan-product-card__media">
                {% if product.featured_image %}
                  {{
                    product.featured_image
                    | image_url: width: 1400
                    | image_tag:
                      loading: 'lazy',
                      widths: '500, 700, 900, 1100, 1400',
                      sizes: '(max-width: 749px) 82vw, 30vw',
                      class: 'hanan-product-card__image'
                  }}
                {% endif %}

                <div class="hanan-product-card__tone"></div>
                <span class="hanan-product-card__view">VIEW PRODUCT →</span>
              </div>

              <div class="hanan-product-card__content">
                <h3 class="hanan-product-card__title">
                  {{ product.title }}
                </h3>

                <span class="hanan-product-card__price">
                  {{ product.price | money }}
                </span>
              </div>
            </a>
          {% endfor %}
        {% endif %}
      </div>
    </div>
  </div>
</section>

<style>
  .hanan-editorial-collection {
    background: #F2ECDD;
    color: #11110C;
    overflow: hidden;
  }

  .hanan-editorial-collection__inner {
    width: 100%;
  }

  .hanan-editorial-collection__top {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 32px;
    padding: 0 5vw;
    margin-bottom: 30px;
  }

  .hanan-editorial-collection__header {
    display: flex;
    align-items: flex-end;
    gap: 22px;
  }

  .hanan-editorial-collection__title {
    margin: 0;
    color: #11110C;
    font-size: clamp(25px, 2.2vw, 36px);
    line-height: 1;
    font-weight: 900;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    font-family: "Arial Narrow", "Helvetica Neue", Arial, sans-serif;
  }

  .hanan-editorial-collection__description {
    max-width: 360px;
    color: rgba(92, 70, 38, 0.72);
    font-size: 12px;
    line-height: 1.65;
    letter-spacing: 0.07em;
    margin-bottom: 1px;
  }

  .hanan-editorial-collection__description p {
    margin: 0;
  }

  .hanan-editorial-collection__controls {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 2px;
  }

  .hanan-editorial-collection__arrow {
    width: 36px;
    height: 36px;
    border-radius: 999px;
    border: 1px solid rgba(17, 17, 12, 0.22);
    background: rgba(250, 247, 242, 0.45);
    color: #11110C;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition:
      background 450ms cubic-bezier(0.16, 1, 0.3, 1),
      color 450ms cubic-bezier(0.16, 1, 0.3, 1),
      border-color 450ms cubic-bezier(0.16, 1, 0.3, 1),
      transform 450ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .hanan-editorial-collection__arrow span {
    display: block;
    font-size: 21px;
    line-height: 1;
    transform: translateY(-1px);
  }

  .hanan-editorial-collection__arrow:hover {
    background: #11110C;
    color: #F2ECDD;
    border-color: #11110C;
    transform: translateY(-1px);
  }

  .hanan-editorial-slider-wrapper {
    overflow-x: auto;
    overflow-y: hidden;
    scroll-behavior: smooth;
    scrollbar-width: none;
    padding-left: 5vw;
    padding-bottom: 12px;
  }

  .hanan-editorial-slider-wrapper::-webkit-scrollbar {
    display: none;
  }

  .hanan-editorial-slider {
    display: flex;
    gap: 30px;
    width: max-content;
    padding-right: 5vw;
  }

  .hanan-product-card {
    width: calc((88vw - 60px) / 3);
    min-width: calc((88vw - 60px) / 3);
    text-decoration: none;
    color: #11110C;
    opacity: 0;
    transform: translateY(26px);
    animation: hananProductReveal 950ms cubic-bezier(0.16, 1, 0.3, 1) forwards;
    animation-delay: calc(var(--delay) * 110ms);
    transition:
      transform 850ms cubic-bezier(0.16, 1, 0.3, 1),
      filter 850ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .hanan-product-card__media {
    position: relative;
    height: clamp(340px, 36vw, 500px);
    overflow: hidden;
    background:
      linear-gradient(145deg, #FAF7F2 0%, #F7F3EA 46%, #ECE5D8 100%);
    box-shadow:
      0 18px 45px rgba(17, 17, 12, 0.08),
      0 2px 10px rgba(17, 17, 12, 0.04);
    transition:
      box-shadow 900ms cubic-bezier(0.16, 1, 0.3, 1),
      transform 900ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .hanan-product-card__image {
    width: 100%;
    height: 100%;
    object-fit: contain;
    object-position: center;
    display: block;
    padding: 16%;
    transform: scale(0.9);
    filter: contrast(1.03) saturate(0.98);
    transition:
      transform 1200ms cubic-bezier(0.16, 1, 0.3, 1),
      filter 1200ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .hanan-product-card__tone {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      radial-gradient(circle at 50% 26%, rgba(255, 255, 255, 0.42), transparent 34%),
      linear-gradient(180deg, rgba(17, 17, 12, 0) 0%, rgba(17, 17, 12, 0.045) 100%);
    opacity: 0.7;
    transition: opacity 850ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .hanan-product-card__view {
    position: absolute;
    left: 24px;
    bottom: 22px;
    color: #11110C;
    font-size: 8.5px;
    line-height: 1;
    font-weight: 800;
    letter-spacing: 0.24em;
    text-transform: uppercase;
    opacity: 0;
    transform: translateY(10px);
    transition:
      opacity 650ms cubic-bezier(0.16, 1, 0.3, 1),
      transform 650ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .hanan-product-card__content {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 22px;
    padding: 22px 2px 0;
  }

  .hanan-product-card__title {
    margin: 0;
    max-width: 68%;
    color: #11110C;
    font-size: 12.5px;
    line-height: 1.55;
    font-weight: 750;
    letter-spacing: 0.055em;
    text-transform: uppercase;
  }

  .hanan-product-card__price {
    color: rgba(17, 17, 12, 0.58);
    font-size: 12px;
    line-height: 1.55;
    letter-spacing: 0.045em;
    white-space: nowrap;
  }

  .hanan-product-card:hover {
    transform: translateY(-4px);
  }

  .hanan-product-card:hover .hanan-product-card__media {
    box-shadow:
      0 26px 64px rgba(17, 17, 12, 0.12),
      0 4px 18px rgba(17, 17, 12, 0.06);
  }

  .hanan-product-card:hover .hanan-product-card__image {
    transform: scale(0.94) translateY(-4px);
    filter: contrast(1.06) saturate(1);
  }

  .hanan-product-card:hover .hanan-product-card__tone {
    opacity: 0.92;
  }

  .hanan-product-card:hover .hanan-product-card__view {
    opacity: 1;
    transform: translateY(0);
  }

  @keyframes hananProductReveal {
    from {
      opacity: 0;
      transform: translateY(26px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media screen and (max-width: 989px) {
    .hanan-editorial-collection__top {
      padding: 0 28px;
      margin-bottom: 28px;
    }

    .hanan-editorial-collection__header {
      gap: 18px;
    }

    .hanan-editorial-slider-wrapper {
      padding-left: 28px;
    }

    .hanan-editorial-slider {
      gap: 22px;
      padding-right: 28px;
    }

    .hanan-product-card {
      width: 44vw;
      min-width: 44vw;
    }

    .hanan-product-card__media {
      height: 420px;
    }
  }

  @media screen and (max-width: 749px) {
    .hanan-editorial-collection__top {
      align-items: flex-start;
      flex-direction: column;
      gap: 18px;
      padding: 0 18px;
      margin-bottom: 26px;
    }

    .hanan-editorial-collection__header {
      align-items: flex-start;
      flex-direction: column;
      gap: 7px;
    }

    .hanan-editorial-collection__title {
      font-size: 27px;
      letter-spacing: 0.095em;
    }

    .hanan-editorial-collection__description {
      font-size: 12px;
      line-height: 1.6;
      max-width: 100%;
    }

    .hanan-editorial-collection__controls {
      align-self: flex-end;
    }

    .hanan-editorial-collection__arrow {
      width: 35px;
      height: 35px;
    }

    .hanan-editorial-slider-wrapper {
      padding-left: 18px;
      padding-bottom: 8px;
    }

    .hanan-editorial-slider {
      gap: 16px;
      padding-right: 18px;
    }

    .hanan-product-card {
      width: 80vw;
      min-width: 80vw;
    }

    .hanan-product-card__media {
      height: 400px;
    }

    .hanan-product-card__image {
      padding: 17%;
      transform: scale(0.9);
    }

    .hanan-product-card__content {
      flex-direction: column;
      gap: 7px;
      padding-top: 16px;
    }

    .hanan-product-card__title {
      max-width: 100%;
      font-size: 12px;
      line-height: 1.5;
    }

    .hanan-product-card__price {
      font-size: 12px;
    }

    .hanan-product-card__view {
      opacity: 1;
      transform: none;
      left: 20px;
      bottom: 20px;
      font-size: 8.5px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hanan-product-card,
    .hanan-product-card__media,
    .hanan-product-card__image,
    .hanan-product-card__tone,
    .hanan-product-card__view,
    .hanan-editorial-collection__arrow {
      animation: none;
      transition: none;
    }

    .hanan-product-card {
      opacity: 1;
      transform: none;
    }
  }
</style>

<script>
  (() => {
    const section = document.querySelector('#hanan-editorial-collection-{{ section.id }}');
    if (!section) return;

    const sliderWrapper = section.querySelector('.hanan-editorial-slider-wrapper');
    const nextButton = section.querySelector('.hanan-editorial-collection__arrow--next');
    const prevButton = section.querySelector('.hanan-editorial-collection__arrow--prev');

    if (!sliderWrapper || !nextButton || !prevButton) return;

    const getScrollAmount = () => {
      const card = section.querySelector('.hanan-product-card');
      if (!card) return sliderWrapper.clientWidth * 0.82;
      const gap = window.innerWidth < 750 ? 16 : window.innerWidth < 990 ? 22 : 30;
      return card.offsetWidth + gap;
    };

    nextButton.addEventListener('click', () => {
      sliderWrapper.scrollBy({
        left: getScrollAmount(),
        behavior: 'smooth'
      });
    });

    prevButton.addEventListener('click', () => {
      sliderWrapper.scrollBy({
        left: -getScrollAmount(),
        behavior: 'smooth'
      });
    });
  })();
</script>

{% schema %}
{
  "name": "HANAN Collection",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "inline_richtext",
      "id": "title",
      "default": "NEW ARRIVALS",
      "label": "Title"
    },
    {
      "type": "richtext",
      "id": "description",
      "default": "<p>Curated sneakers and apparel.</p>",
      "label": "Description"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "min": 3,
      "max": 12,
      "step": 1,
      "default": 6,
      "label": "Products to show"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 30,
      "max": 140,
      "step": 5,
      "unit": "px",
      "default": 70,
      "label": "Top spacing"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 40,
      "max": 160,
      "step": 4,
      "unit": "px",
      "default": 92,
      "label": "Bottom spacing"
    }
  ],
  "presets": [
    {
      "name": "HANAN Collection"
    }
  ]
}
{% endschema %}